import { NgModule } from '@angular/core';
import { Routes, RouterModule } from '@angular/router';
import { StudiesComponent } from './studies.component';
import { StudySummaryComponent } from './study-summary/study-summary.component';

/*
const routes: Routes = [
  {
    path: '',
    component: StudiesComponent,
    data: {
      title: 'Studies'
    }
  },
  {
    path: ':id/view',
    component: StudySummaryComponent,
    data: {
      title: 'Study Details'
    }
  }
];

@NgModule({
  imports: [RouterModule.forChild(routes)],
  exports: [RouterModule]
})
export class StudiesRoutingModule {}

*/ //hay que cambiar la estrucutre del proyecto para que esto funcione
